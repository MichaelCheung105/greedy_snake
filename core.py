import numpy as np


class initializer():
    def __init__(self):
        self.height = 40
        self.width = 20
        arena = np.zeros(self.height * self.width, dtype=int).reshape((self.height, self.width))
        arena[]
        self.arena =

    def initialize(self, height=40, width=20):
        self.height = height
        self.width = width
        self.arena = np.concatenate((np.ones(width), np.))